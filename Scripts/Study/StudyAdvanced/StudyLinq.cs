using System.Collections.Generic;
using UnityEngine;

public class StudyLinq : MonoBehaviour
{
    // LINQ 

    // 配列やList、Dictionaryなどの要素を処理するメソッドを集めた便利機能
    // メソッドチェイン（関数をドットで繋げて処理を書くもの）で記述できる

    // メリット
    // コードの可読性が上がる
    // バグが発生しにくい

    // デメリット
    // 勉強が必要
    // 処理が必ずしも早くなるとは限らない(AIに聞けば教えてくれる)

    // IEnumerable 
    private List<int> List = new();
    private Dictionary<int, int> Dictionary = new();
    // データを大量に扱うクラスが共通して継承しているインターフェース

    // デリゲートやラムダを使用する。
}
